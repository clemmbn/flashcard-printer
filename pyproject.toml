[project]
name = "flashcard-printer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "pillow>=12.2.0",
    "playwright>=1.60.0",
    "python-escpos>=3.1",
]
